/*
Para aplicar el SRP, se puede dividir la clase GestorArchivos en dos clases separadas: 
una para la lectura de archivos y otra para la escritura de archivos de la siguiente manera:
*/

class GestorLecturaArchivo {
    leerArchivo(ruta) {
        let contenido = "Contenido por defecto";
        // Lógica para leer archivo (simulada)
        return contenido;
    }
}

class GestorEscrituraArchivo {
    escribirArchivo(ruta, contenido) {
        // Lógica para escribir archivo (simulada)
    }
}